# DESCRIPTION: finding the area of a triangle

height = input("Enter the height: ")
base = input("Enter the base: ")

area = float(0.5) * float(height) * float(base) # multiply the area

print('')
print(f"The area of the triangle is {area:.1f}") 